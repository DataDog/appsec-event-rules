id: ua0-600-7xx
name: SQLmap
tags:
  type: security_scanner
conditions:
  - operation: match_regex
    parameters:
      inputs:
        - $http.user_agent
      regex: ^sqlmap/
transformers: []
action: record
