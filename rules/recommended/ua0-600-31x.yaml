id: ua0-600-31x
name: Nikto
tags:
  type: security_scanner
conditions:
  - operation: match_regex
    parameters:
      inputs:
        - server.request.headers.no_cookies:user-agent
      regex: \(Nikto/[\d\.]+\)
transformers: []
action: record
