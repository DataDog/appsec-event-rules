id: ua0-600-31x
name: Nikto
tags:
  type: security_scanner
conditions:
  - operation: match_regex
    parameters:
      inputs:
        - $http.user_agent
      regex: \(Nikto/[\d\.]+\)
transformers: []
action: log
