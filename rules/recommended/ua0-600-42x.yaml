id: ua0-600-42x
name: OpenVAS
tags:
  type: security_scanner
conditions:
  - operation: match_regex
    parameters:
      inputs:
        - $http.user_agent
      regex: (?i)OpenVAS\b
transformers: []
action: record
