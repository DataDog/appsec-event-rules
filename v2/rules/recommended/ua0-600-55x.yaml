id: ua0-600-55x
name: Datadog test scanner
tags:
  type: security_scanner
  category: attack_attempt
conditions:
  - parameters:
      inputs:
        - address: server.request.headers.no_cookies
          key_path:
            - user-agent
      regex: ^dd-test-scanner-log$
    operator: match_regex
transformers: []
